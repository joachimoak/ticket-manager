// import request from "supertest";
// // import testDbConnector from "../../config/test-db-connector";
// // import serverStarter, { app } from "../../config/server-starter";
// // import http from 'http';
// import { createUser, deleteUser, editUser, getUserById, getUsers } from "./user-controller";
// import { Request, Response } from "express";

// describe("GET /api/users", () => {
//     test("It should respond with an array of users", async () => {

//         // const response = await request(app).get("/api/users");
//         // console.log(response.body)
//         // expect(response.body.length).toBe(2);
//         // expect(response.body[0]).toHaveProperty("id");
//         // expect(response.body[0]).toHaveProperty("firstName");
//         // expect(response.body[0]).toHaveProperty("lastName");
//         // expect(response.body[0]).toHaveProperty("email");
//         // expect(response.statusCode).toBe(200);
//         // expect(getUsers()).toBe([]);
//     });
// });
